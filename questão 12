#include <iostream>
using namespace std;

void pont(int num) {
    int *ptr = &num;
    *ptr = *ptr * 2;
    cout << *ptr << endl;
}

int main() {
    int numero1;
    
    cout << "Informe um valor: ";
    cin >> numero1;
    
    pont(numero1);
    return 0;
}
